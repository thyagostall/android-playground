<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="@string/pref_sorting_key"
        android:summary="Changes the ordering which the movies appear at the main screen"
        android:title="@string/pref_movieSorting"
        android:dialogTitle="@string/pref_movieSorting"
        android:entries="@array/pref_sorting"
        android:entryValues="@array/pref_sorting_values"
        android:defaultValue="@string/pref_sorting_popularity" />

</PreferenceScreen>